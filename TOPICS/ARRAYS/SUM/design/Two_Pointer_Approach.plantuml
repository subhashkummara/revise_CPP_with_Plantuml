@startuml Two_Pointer_Approach
!theme plain

title Two Pointer Approach (Target = 10)

start

:Sorted Array: [1,2,3,4,5,6,7,8,9];
:left = 0, right = 8;

while (left < right?) is (yes)
  :sum = arr[left] + arr[right];
  
  if (sum == target?) then (yes)
    :Found pair!;
    :Return [left, right];
    stop
  elseif (sum < target?) then (yes)
    :left++;
  else (sum > target)
    :right--;
  endif
endwhile (no)

:No pair found;
stop

note right
  Time: O(n)
  Space: O(1)
  Array must be sorted!
end note

@enduml